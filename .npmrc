# Optimize package installation for better tree-shaking
# Use exact versions (no semver ranges)
save-exact=true

# Prefer production dependencies (no dev deps in production)
production=false

# Enable package-lock for reproducible builds
package-lock=true

# Optimize node_modules structure
legacy-peer-deps=false
